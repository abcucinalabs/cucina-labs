# Supabase
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Database (legacy — will be removed after migration)
DATABASE_URL="postgresql://user:password@localhost:5432/cucina_labs?schema=public"

# NextAuth (legacy — will be removed after migration)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-random-32-plus-chars"

# Public URL (for production short links - use your production domain)
NEXT_PUBLIC_BASE_URL="https://cucinalabs.com"

# App secrets
ENCRYPTION_KEY="replace-with-32-char-hex"
CRON_SECRET="replace-with-random-secret"

# External APIs
GEMINI_API_KEY=""
RESEND_API_KEY=""
